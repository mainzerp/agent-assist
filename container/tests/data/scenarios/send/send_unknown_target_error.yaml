id: send.send_unknown_target_error
agent: send-agent
description: Unknown send target produces an entity-not-found-style error response
snapshot: home_default
language: en
context: {source: ha, area_id: living_room, device_id: satellite_living_room}
llm:
  classify: |
    send-agent (95%): Bob Phone|||CONTENT|||Hello.
  agents:
    orchestrator: |
      general-agent (95%): draft the hello message
      send-agent (95%): Bob Phone|||CONTENT|||Hello.
    general-agent: |
      Hello.
    send-agent: |
      Hello.
request:
  text: "send hello to Bob phone"
expected:
  routed_agent: general-agent, send-agent
  service_calls: []
  speech_contains: ["mapping"]
