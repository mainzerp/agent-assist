id: scene.query_scene_state
agent: scene-agent
description: Query the state of the movie night scene
snapshot: home_default
language: en
context: {source: ha, area_id: living_room, device_id: satellite_living_room}
llm:
  classify: |
    scene-agent (95%): Query state of the movie night scene
  agents:
    scene-agent: |
      Checking the movie night scene.
      ```json
      {"action": "query_scene", "entity": "movie night", "parameters": {}}
      ```
request:
  text: "is the movie night scene set?"
expected:
  routed_agent: scene-agent
  service_calls: []
  speech_contains: ["movie"]
