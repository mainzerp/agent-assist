id: timer.de_recurring_alarm_daily
agent: timer-agent
description: German daily recurring alarm setup via internal scheduler
snapshot: home_default
language: de
context: {source: ha, area_id: bedroom, area_name: "Schlafzimmer", device_id: satellite_bedroom}
llm:
  classify: |
    timer-agent (95%): Täglichen Wecker um 7 Uhr stellen
  agents:
    timer-agent: |
      Ich stelle den täglichen Wecker auf 7 Uhr.
      ```json
      {"action": "set_datetime", "entity": "Wecker", "parameters": {"time": "07:00:00", "recurrence": {"freq": "daily"}}}
      ```
request:
  text: "stelle jeden tag um sieben uhr einen wecker"
expected:
  routed_agent: timer-agent
  service_calls: []
  speech_contains: ["scheduler"]
