id: security.motion_sensor_status
agent: security-agent
description: Check the living room motion sensor status
snapshot: home_default
language: en
context: {source: ha, area_id: living_room, device_id: satellite_living_room}
llm:
  classify: |
    security-agent (95%): Query the living room motion sensor
  agents:
    security-agent: |
      Checking motion in the living room.
      ```json
      {"action": "query_security_state", "entity": "living room motion", "parameters": {}}
      ```
request:
  text: "is there motion in the living room?"
expected:
  routed_agent: security-agent
  service_calls: []
  speech_contains: ["living"]
