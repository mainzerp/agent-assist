id: automation.trigger_morning_routine
agent: automation-agent
description: "Trigger the morning routine automation"
snapshot: home_default
language: en

context:
  source: ha
  area_id: kitchen
  device_id: satellite_kitchen

llm:
  classify: |
    automation-agent (95%): Trigger the morning routine automation
  agents:
    automation-agent: |
      Triggering morning routine.
      ```json
      {"action": "trigger_automation", "entity": "morning routine", "parameters": {}}
      ```

expected:
  routed_agent: automation-agent
  service_calls:
    - domain: automation
      service: trigger
      target_entity: automation.morning_routine
  speech_contains: ["morning"]
