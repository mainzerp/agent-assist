id: automation.list_automations
agent: automation-agent
description: List all automations
snapshot: home_default
language: en
context: {source: ha, area_id: living_room, device_id: satellite_living_room}
llm:
  classify: |
    automation-agent (95%): List all automations
  agents:
    automation-agent: |
      Listing all automations.
      ```json
      {"action": "list_automations", "entity": "", "parameters": {}}
      ```
request:
  text: "list my automations"
expected:
  routed_agent: automation-agent
  service_calls: []
  speech_contains: ["morning"]
