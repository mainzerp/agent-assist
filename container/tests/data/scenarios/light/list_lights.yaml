id: light.list_lights
agent: light-agent
description: List all lights and switches in the home
snapshot: home_default
language: en
context: {source: ha, area_id: living_room, device_id: satellite_living_room}
llm:
  classify: |
    light-agent (95%): List all lights
  agents:
    light-agent: |
      Listing all lights.
      ```json
      {"action": "list_lights", "entity": "", "parameters": {}}
      ```
request:
  text: "which lights are on?"
expected:
  routed_agent: light-agent
  service_calls: []
  speech_contains: ["lights"]
