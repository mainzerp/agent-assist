id: light.illuminance_sensor_garden
agent: light-agent
description: Read the garden illuminance sensor through the light agent
snapshot: home_default
language: en
context: {source: ha, area_id: garden, device_id: satellite_living_room}
llm:
  classify: |
    light-agent (95%): Query the garden illuminance sensor
  agents:
    light-agent: |
      Reading the garden illuminance sensor.
      ```json
      {"action": "query_light_state", "entity": "garden illuminance", "parameters": {}}
      ```
request:
  text: "how bright is it in the garden?"
expected:
  routed_agent: light-agent
  service_calls: []
  speech_contains: ["garden"]
