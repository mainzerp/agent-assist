id: light.toggle_bedroom
agent: light-agent
description: Toggle the bedroom ceiling light
snapshot: home_default
language: en
context: {source: ha, area_id: bedroom, device_id: satellite_bedroom}
preconditions:
  entity_overrides:
    - {entity_id: light.bedroom_ceiling, state: "off"}
llm:
  classify: |
    light-agent (95%): Toggle the bedroom ceiling light
  agents:
    light-agent: |
      Toggling the bedroom ceiling light.
      ```json
      {"action": "toggle", "entity": "bedroom ceiling", "parameters": {}}
      ```
request:
  text: "toggle the bedroom ceiling light"
expected:
  routed_agent: light-agent
  service_calls:
    - {domain: light, service: toggle, target_entity: light.bedroom_ceiling}
  speech_contains: ["bedroom"]
